services:
  extract:
    build: .
    env_file:
      - local.env
    volumes:
      - ./files:/app/files
  test:
    build:
      dockerfile: test.Dockerfile
    volumes:
      - ./tests:/app/tests
      - ./files:/app/files/
