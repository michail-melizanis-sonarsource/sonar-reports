{
  "url": "api/webhooks/deliveries",
  "dependencies": [
    {
      "type": "entity",
      "key": "project_webhooks"
    }
  ],
  "resultKeys": [
    "deliveries"
  ],
  "totalKey": "$.paging.total",
  "paginationKey": "p",
  "pageSizeKey": "ps",
  "pageLimit": 10,
  "maxPageSize": 500,
  "parameters": {
    "webhooks": {
      "source": "project_webhooks",
      "path": "$.key"
    }
  },
  "operations": [
    {
      "operator": "set",
      "key": "webhookKey",
      "value": {
        "source": "project_webhooks",
        "path": "$.key"
      }
    }
  ],
  "mappings": [
    {
      "destination": "webhook_delivery",
      "source": "$",
      "uniqueKeys": [
        "webhook_delivery_id"
      ],
      "columns": [
        {
          "name": "webhook_delivery_id",
          "path": "$.id"
        },
        {
          "name": "webhook_key",
          "path": "$.webhookKey"
        },
        {
          "name": "response_code",
          "path": "$.httpStatus"
        },
        {
          "name": "created_ts",
          "path": "$.at"
        }
      ]
    }
  ]
}
